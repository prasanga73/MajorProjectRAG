# Core dependencies for Legal RAG System V2 with Reranker

# Embedding and ML
sentence-transformers>=2.2.0
torch>=2.0.0
transformers>=4.30.0

# Scientific computing
numpy>=1.24.0
scikit-learn>=1.3.0

# Utilities
tqdm>=4.65.0

# API Server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# HTTP Client
requests>=2.31.0

# Optional: For better performance
faiss-cpu>=1.7.4  # For fast similarity search (use faiss-gpu for GPU support)

# Development
pytest>=7.4.0
black>=23.0.0
httpx>=0.25.0  # For testing FastAPI

# Note: For best reranking performance, install sentence-transformers with cross-encoder support
# The package includes both bi-encoder (for embeddings) and cross-encoder (for reranking)
